# ChronoFlow Environment Configuration
# Copy this file to .env and fill in your values

# ===================
# Required in Production
# ===================

# GitHub repository for pulling images (e.g., your-username/chronoflow)
GITHUB_REPO=your-username/chronoflow

# JWT secret for signing tokens (generate with: openssl rand -base64 32)
JWT_SECRET=your-secure-secret-here

# ===================
# Cloudflare Tunnel (Optional)
# ===================
# Required only when using: --profile tunnel
# Get token from Cloudflare Zero Trust dashboard
# CLOUDFLARE_TUNNEL_TOKEN=your-tunnel-token-here

# Host port for direct access (default: 4849)
# HOST_PORT=4849

# ===================
# Optional Settings
# ===================

# Server port (default: 4849)
# PORT=4849

# Database path (default: /app/data/timetracker.db in Docker)
# DB_PATH=/app/data/timetracker.db

# CORS allowed origins (comma-separated, or * for all)
# CORS_ORIGIN=https://chronoflow.yourdomain.com

# Rate limiting (requests per minute per IP)
# RATE_LIMIT_MAX=100
# RATE_LIMIT_WINDOW_MS=60000

# JWT token expiration
# JWT_EXPIRES_IN=15m
# REFRESH_TOKEN_EXPIRES_IN=7d

# Trust proxy headers (set to true behind reverse proxy/tunnel)
# TRUST_PROXY=true

# Maximum request body size
# MAX_REQUEST_SIZE=1mb

# Database auto-save interval in milliseconds
# DB_AUTO_SAVE_INTERVAL=5000
